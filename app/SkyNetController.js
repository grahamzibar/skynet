/*

SkyNetView Controller
by Graham Robertson

Receives events from the model (SkyNet) and updates the view (SkyNetView) as
well as receives input event from the user via the view and corresponds that to
actions to be performed upon the model.

*Requires*
	SkyNetModel
	SkyNetView

*/
function SkyNetController = function(model, view) {
	
};